import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.heliosalles.organizza',
  appName: 'Organizza',
  webDir: 'dist',
  server: {
    iosScheme: 'https',
  },
};

export default config;
